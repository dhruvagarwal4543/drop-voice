import 'package:flutter_test/flutter_test.dart';

void main() {
  // Integration tests (requiring Firebase + LiveKit) are in test/integration/.
  // This placeholder ensures `flutter test` exits successfully.
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
